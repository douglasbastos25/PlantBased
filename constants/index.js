import images from "./images";
import icons from "./icons";
import { COLORS, FONT, SIZES, SHADOWS, FONT_SIZES } from "./theme";

const DEBUG = false;

export { images, icons, COLORS, FONT, SIZES, SHADOWS, DEBUG, FONT_SIZES };
